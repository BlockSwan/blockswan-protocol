{
  "name": "@blockswan/contract-helpers",
  "version": "0.0.1",
  "description": "Helper functions for interacting with blockswan periphery & protocol contracts",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "typechain": "cp -r ../../contracts/abi/ tmp && rm ./tmp/abi.json && typechain --target=ethers-v5 --out-dir=./src/typechain/ './tmp/**/*.json' && rm -rf tmp"
  },
  "author": "blockswan",
  "license": "MIT",
  "devDependencies": {
    "@typechain/ethers-v5": "^10.2.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "typechain": "^8.1.1",
    "typescript": "^4.9.5"
  }
}
